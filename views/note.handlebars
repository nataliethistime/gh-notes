<div class="header">
  <h1>{{formatTitle fName}}</h1>
  <h2>{{pageSubtitle}}</h2>
</div>

<div class="content">
  {{> dark-mode-toggle}}
  {{{noteHtml}}}
  {{#if config.githubNotesLink}}
    <hr />
    <a href="{{config.githubNotesLink}}{{noteLocation}}" target="_blank" rel="noopener noreferer">
      View in GitHub
    </a>
  {{/if}}
</div>
